 class Main {

 /* The owl of Minerva spreads its wings only with the falling of the
 dusk.
// Another single-line comment
  */

	 Void main(String a, String b, Bool c, Int d){

	 	Bool e;
		Int f;

	    while(c){

	       new_string = a + b;
		   f = d * 2 / 7 + 15;
		   if (b && (d >= f) || (e || c)) {
		   		e=false;
		   } else {
		   		e=true;
		   }
	    }
	 }

 }

 class OrderMacs {
 	Bool c;
 	Int i;

 	Int orders() {

 	  Int j;

 	  if (i<10) {
 	  	println("Order Macs");
        readln(i);
 	  } else {
	  	println("Do not order Macs");
	  }

   }
 }
